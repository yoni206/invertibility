((Start (BitVec 4) (
     s
     t
     #x0
     #x8 ; min_val
     #x7 ; max_val
     (bvneg  Start)
     (bvnot  Start)
     (bvadd  Start Start)
     (bvsub   Start Start)
     (bvmul   Start Start)
   ))
)
